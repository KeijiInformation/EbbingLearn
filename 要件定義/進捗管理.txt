Goals > uid_final1 に保存するデータに進捗に関わるデータを追加する。具体的には以下の通りである。
    1. 小目標に進捗に関わるデータを作成
        ・終了しているかどうかを管理するフラグ
        ・行った量を保存する数値
        ・日付事にどのタスクを行ったかを保存するオブジェクト
    2. 小目標の進捗を更新する
        ・上の3つを更新する処理を書けばよい
    3. 小目標の進捗度を計算する
        ・終了済みの量 * タスクジャンルと単位から算出される重み によって割合を計算する
        ・終了済みの量はデータベースから取得する
        ・タスクジャンルと単位から算出される重みは以下のように定義する
            - 動画: 分=5, 本=50
            - 本: ページ=2, 章=60
            - 講座: 講座=60

ProgressDataによって管理する
    1. 小目標に進捗に関わるデータを作成
        (Goals >> small1 に追加)
            ・終了しているかどうかを管理するフラグ
            ・行った量を保存する数値
        (ProgressData >> small1 に追加)
            ・その日にやった量
            ・どこからどこまでやるか
            ・その日にやる量
            ・重み
            ・id文字列
    2. 小目標の進捗を更新する
        ・上から4つを更新する
    3. 小目標の進捗度を計算する
        ・終了済みの量 * タスクジャンルと単位から算出される重み によって割合を計算する
        ・終了済みの量はデータベースから取得する
        ・タスクジャンルと単位から算出される重みは以下のように定義する
            - 動画: 分=5, 本=50
            - 本: ページ=2, 章=60
            - 講座: 講座=60